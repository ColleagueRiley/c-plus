- bugs
- multi namespace
- struct in a namespace v
- multi structs v
- namespace '.'

- object functions
    replace object.function(args) with _cplus_class_function(object, args);
    this->
    load data from this-> 
    make it so it only loads data when it is used per function

- pointer objects
    obj->func(obj);

- void* objects
    void* a;
    ((class*)a)->func()
    *((class)a).func()

- embeded structs 


operator overloading

- handle pre-processors
- header files

(maybe)
string switches

- multi argument system 

{int, int} func -> func(int&, int&)

for in loop 

- ADD COMMENTS