name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macOS-10.14

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
